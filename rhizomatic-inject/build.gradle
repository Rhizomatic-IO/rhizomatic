def guiceVersion = '5.1.0'

dependencies {
    api project(':rhizomatic-api')
    implementation project(':rhizomatic-kernel')

    implementation("com.google.inject:guice:${guiceVersion}") {
        exclude group: 'com.google.code.findbugs'
    }

}

